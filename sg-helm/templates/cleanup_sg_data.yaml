apiVersion: batch/v1
kind: Job
metadata:
  name: "{{ template "fullname" . }}-cleanup-sg-data"
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    role: sgadmin
  annotations:
    # This is what defines this resource as a hook. Without this line, the
    # job is considered part of the release.
    "helm.sh/hook": post-delete
    "helm.sh/hook-weight": "-1"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  template:
    metadata:
      name: "{{ template "fullname" . }}-cleanup-sg-data"
      labels:
        release: "{{ .Release.Name }}"
        app: {{ template "fullname" . }}
        component: {{ template "fullname" . }}
        role: sgadmin
    spec:
      restartPolicy: Never
      serviceAccountName: {{ template "fullname" . }}
      containers:
        - name: cleanup-sg-data
          image: bitnami/kubectl:1.19
          imagePullPolicy: {{ .Values.common.pullPolicy }}
          command:
            - /bin/sh
            - -c
            - |
{{ if .Values.common.remove_pvc_on_delete }}
                kubectl delete $$(kubectl get pvc -l component={{ template "fullname" . }} -o name)
{{ end }}
                kubectl delete secrets "{{ .Release.Name }}"-ingress-nginx-admission

                kubectl delete $$(kubectl get secrets -l app={{ template "fullname" . }} -o name)