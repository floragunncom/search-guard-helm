{{- /*
    Includes code from the following Apache 2 licensed projects:

      - https://github.com/lalamove/helm-elasticsearch

        Licensed under the Apache License, Version 2.0 (the "License");
   you may not use this file except in compliance with the License.
   You may obtain a copy of the License at

   http://www.apache.org/licenses/LICENSE-2.0

   Unless required by applicable law or agreed to in writing, software
   distributed under the License is distributed on an "AS IS" BASIS,
   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   See the License for the specific language governing permissions and
   limitations under the License.
*/}}


{{- $clientValues := .Values.client }}
{{- $single := false }}
{{- $createSharedService := kindIs "invalid" .Values.common.clientCreateSharedService | ternary true .Values.common.clientCreateSharedService }}
{{- if not (kindIs "slice" .Values.client) }}
{{- if $createSharedService }}
{{- $clientValues = list (merge (dict "svcType" "shared") .Values.client) }}
{{- else }}
{{- $clientValues = list (merge (dict "name" "client") .Values.client) }}
{{- end }}
{{- $single = true }}
{{- else }}
{{- if $createSharedService }}
{{- $clientValues = append $clientValues (dict "svcType" "shared") }}
{{- end }}
{{- end }}
{{- range $clientValues }}

{{- $isShared := eq .svcType "shared" }}
{{- $clientGroupName := "" }}
{{- if $isShared }}
{{-   $clientGroupName = printf "%s-clients" (include "searchguard.fullname" $) }}
{{- else }}
{{-   $usePlural := .pluralName | default false }}
{{-   $baseName := "" }}
{{-   if $usePlural }}
{{-     $baseName = "clients" }}
{{-   else }}
{{-     $baseName = "client" }}
{{-   end }}
{{-   $useBaseNameOnly := or $single (or .baseNameOnly .single) }}
{{-   $clientGroupSuffix := "" }}
{{-   if $useBaseNameOnly }}
{{-     $clientGroupSuffix = $baseName }}
{{-   else }}
{{-     $clientGroupSuffix = printf "%s-%s" $baseName .name }}
{{-   end }}
{{-   $fullname := include "searchguard.fullname" $ }}
{{-   $clientGroupName = printf "%s-%s%s" $fullname (.prefix | default "") $clientGroupSuffix }}
{{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $clientGroupName }}
  namespace: {{ $.Release.Namespace }}
  labels:
    app: {{ template "searchguard.fullname" $ }}
    chart: "{{ $.Chart.Name }}"
    release: "{{ $.Release.Name }}"
    heritage: "{{ $.Release.Service }}"
    component: {{ template "searchguard.fullname" $ }}
    role: client
    group: {{ $clientGroupName }}
  {{ $serviceType := .serviceType | default $.Values.common.serviceType }}
  {{ $annotations := .service_annotations | default $.Values.common.service_annotations }}
  {{- if and $annotations (eq $serviceType "LoadBalancer") }}
  annotations:
    {{- $annotations | toYaml | nindent 4 }}
  {{- end }}
spec:
  type: {{ $serviceType }}
  {{- $loadBalancerIP := .loadBalancerIp | default $.Values.common.loadBalancerIp }}
  {{- if and $loadBalancerIP (eq $serviceType "LoadBalancer") }}
  loadBalancerIP: {{ $loadBalancerIP }}
  {{- end }}
  selector:
    component: {{ template "searchguard.fullname" $ }}
    role: client
    {{- if not $isShared }}
    group: {{ $clientGroupName }}
    {{- end }}
  ports:
  - name: http
    port: {{ $.Values.service.httpPort }}
    targetPort: 9200
    protocol: TCP
    {{- $nodePort := .nodePort | default $.Values.common.nodePort }}
    {{- if and $nodePort (eq $serviceType "NodePort") }}
    nodePort: {{ $nodePort }}
    {{- end }}
  - name: transport
    port: {{ .transportPort | default $.Values.service.transportPort }}
    targetPort: 9300
    protocol: TCP
{{- $sourceRanges := .loadBalancerSourceRanges | default $.Values.common.loadBalancerSourceRanges }}
{{- if and $sourceRanges (eq $serviceType "LoadBalancer") }}
  loadBalancerSourceRanges:
    {{- toYaml $sourceRanges | nindent 4 }}
{{- end }}
{{- end }} 