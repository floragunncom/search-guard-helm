apiVersion: v1
kind: Service
metadata:
  name: {{ template "searchguard.fullname" . }}-discovery
  labels:
    app: {{ template "searchguard.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    component: {{ template "searchguard.fullname" . }}
    role: master
spec:
  selector:
    component: {{ template "searchguard.fullname" . }}
    role: master
  ports:
  - name: transport
    port: 9300
    protocol: TCP
